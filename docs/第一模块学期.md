# 第一模块学期相关逻辑

## 已注释

| 位置 | 逻辑 | 状态 |
| --- | --- | --- |
| `src/views/redrawPages/teacherHomePage.vue` | `History` 历史学期组件入口 | 已注释 `<History />` |
| `src/views/redrawPages/teacherHomePage.vue` | 右上角菜单：查看历史学期/返回当前学期 | 已注释菜单项 |
| `src/views/redrawPages/teacherHomePage.vue` | `History` 组件 import | 已注释 |
| `src/components/admin/CreateTeachUnit.vue` | 教学单位复制历史学期入口 | 按钮已禁用 |

## 仍保留

考虑到后续改造此处仅做注释不删除代码

| 位置 | 逻辑 | 说明 |
| --- | --- | --- |
| `src/components/History/History.vue` | `/homes/history`、`/login/user`，请求体包含 `termid` | 组件代码保留，但入口已注释 |
| `src/stores/useHistory.js` | History store：`historyList`、`nowUsr`、`pastTerm` | 依赖 History 入口 |
| `src/api/history.js` | `/homes/history`、`/login/user` 封装 | 依赖 History 入口 |
| `src/components/admin/CreateTeachUnit.vue` | `allterm`、`copyTerm` 相关函数 | 函数保留，按钮已禁用 |
| `src/components/admin/Termmangt.vue` | 学期管理：增删改查、设置当前学期、写 `currentTermId` | 完整保留 |
| `src/views/redrawPages/Login.vue` | 请求 `/coursemangt/course/currenttermId` | 用于判断是否存在当前学期 |
| `src/stores/InstructorStore.js` | `isDefaultTerm` | 登录页/首页/学期管理使用 |
| `src/stores/profileStore.js` | `currentterm` | 仅保存和展示当前学期名称 |

## 结论

- 历史学期登录入口已关闭，正常 UI 不会触发 `History.vue` 的 `termid` 请求。
- 教学单位历史复制入口已关闭。
- 学期管理和当前学期展示仍保留。
